#include <iostream>   // Includes the library for input/output (cout, endl)
using namespace std;  // Allows using standard library names without "std::"
void hello (){    // Defines a function named `hello1` that takes no parameters
    cout << "Hello" << endl; // Prints "Hello" followed by a newline 
}

int main(){   // Start of the main function (program entry point) 

    hello();  // Calls the `hello` function to output "Hello"

    return 0;  // Returns 0 to indicate successful program execution 
    
}